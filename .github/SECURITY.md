# Security

This is a template — it ships no runtime service. Still, if you find a security
issue (e.g. a hook or settings pattern that could leak secrets), please open a
private report via GitHub Security Advisories on this repo rather than a public
issue. Never include real credentials in reports.
